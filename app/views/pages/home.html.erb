<div class="jumbotron text-center">
  <h1>Welcome to Devmatch</h1>
  <h3>A place where entrepreneurs can meet developers</h3>
</div>
<div class="row">
  <% if current_user %>
    <div class="col-md-6">
      <div class="well text-center">
        <h1>Your Profile</h1>
        <% if current_user.profile %>          
          <%= link_to 'View your profile', user_path(current_user), class: 'btn btn-default btn-lg btn-block' %>
          <%= link_to 'Update your profile', edit_user_profile_path(current_user), class: 'btn btn-default btn-lg btn-block' %>
        <% else %>
          <p>Create your profile to join, get known, and get updated by others in the community</p> <br/>
          <%= link_to 'Create your profile', new_user_profile_path(current_user), class: 'btn btn-default btn-lg btn-block' %> 
        <% end %>
      </div>
    </div>
    <div class="col-md-6">
      <div class="well text-center">
        <h1>The Community</h1>
          <p>Visit the community to meet developers, entrepreneurs, and investors, <br/>start your journey with others!</p>
          <%= link_to 'Visit the Community', users_path, class: 'btn btn-default btn-lg btn-block' %>
      </div>
    </div>
  <% else %>
    <div class="col-md-6">
    	<div class="well text-center">
    	  <h2>Basic Member</h2>
    	  <h4>Sign up for free and get basic access to the community!</h4>
    	  <br/>
    	  <%= link_to 'Sign up free', new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-lg btn-block' %>
    	</div>
    </div>
    <div class="col-md-6">
    	<div class="well text-center">
    	  <h2>Pro Member</h2>
    	  <h4>Sign up for the pro account for $10/month and get access to the community and their contact information!</h4>
    	  <%= link_to 'Go Pro', new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-lg btn-block' %>
    	</div>
    </div>
  <% end %>
</div>